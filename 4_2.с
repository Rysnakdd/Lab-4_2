#include <stdio.h>
#include <math.h>
#include <locale.h>
int main()
{
	setlocale(LC_CTYPE, "ukr");
	int s = 0, d = 1;
	for (int i = 1; i <= 10, i++)
	{
		if (i % 2 == 0)
			s += i;
	}
	d = s * s;
	printf("Квадрат суми всіх парних чисел у діапазоні [1;10] = %d",s)


	return 0;
}
